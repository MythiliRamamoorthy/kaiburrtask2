apiVersion: v1
kind: Service
metadata:
  name: kaiburr-service
spec:
  type: NodePort
  selector:
    app: kaiburr-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
